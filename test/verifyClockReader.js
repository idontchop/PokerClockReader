const assert = require('assert')
import ClockReader from '../clock-reader'

describe('Clock Reader Test', function () {
    
    it('should initiate', () => {
        let t = new ClockReader()
        t.readImage("./images/1.jpg")
    }),
    it('test chipCount', () => {
        //let t = new TourneyChop(5000,5000,6)
        
    })
})